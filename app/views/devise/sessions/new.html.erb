<h2><%= t 'misc.title.sign_in' %></h2>

<%= semantic_form_for resource_name, :url => session_path(resource_name) do |f| %>

  <%= devise_error_messages! %>
  
  <%= f.inputs :email, :password %>
  <%= f.buttons do %>
    <%= f.commit_button %>
  <% end %>
<% end %>

<%= render "links" %>
