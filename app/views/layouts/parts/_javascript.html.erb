<%= javascript_include_tag :application %>
<%= yield :javascripts %>

<script>

  window.routes = {
  availabilities : {
  create : '<%= table_dashboard_availability_create_path %>',
  update : '<%= table_dashboard_availability_update_path %>',
  del : '<%= table_dashboard_availability_delete_path %>'
  }

  }
  
  window.translations = {
  calendar : {
  }
  }

  window.images = {
  spinner : '<%= image_tag 'spinner.gif' %>'
  }
</script>

<% if Rails.env.production? -%>
  <script>
    var _gaq = [['_setAccount', 'UA-XXXXX-X'], ['_trackPageview']];
    (function(d, t) {
    var g = d.createElement(t),
    s = d.getElementsByTagName(t)[0];
    g.async = true;
    g.src = ('https:' == location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    s.parentNode.insertBefore(g, s);
    })(document, 'script');
  </script>
<%- end -%>
