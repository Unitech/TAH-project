

<!-- Main hero unit for a primary marketing message or call to action -->
<div class="hero-unit">
  <h1>Welcome to the TAH project</h1>
  <br/>
  <% if !user_signed_in? %>
    <p><a class="btn btn-primary btn-large" href="<%= new_user_registration_path %>">Register »</a></p>
  <% end %>
</div>

<!-- Example row of columns -->
<div class="row">
  <div class="span8">
    <h2>Table listing</h2>
    <ul>
      <% @tables.each do |table| %>
	<li>
	  <%= link_to table.title, table_view_show_path(table) %>
	</li>
      <% end %>
  </div>
  <div class="span4">
    <h2>Sidebar</h2>
  </div>
</div>

<hr>

</div>
